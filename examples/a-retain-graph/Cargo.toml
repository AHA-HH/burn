[package]
name = "a-retain-graph"
version = "0.1.0"
edition = "2021"

[dependencies]
# burn = { version = "0.20", features = ["autodiff", "ndarray"] }
burn = { path = "../../crates/burn", features = ["autodiff", "ndarray"] }